interface:
  display_name: "TIMECODE-AGENT"
  short_description: "Video recall, transcription, verification, and edit handoff"
  default_prompt: "Use $timecode-agent to analyze a supplied video or retrieve timestamped evidence from the existing video-agent corpus, following the user's requested output."
policy:
  allow_implicit_invocation: true
